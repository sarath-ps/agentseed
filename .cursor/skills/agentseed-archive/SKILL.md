# AgentSeed Archive

Merge accepted change deltas into the living specs under `agentseed/specs/` and move the change folder to `agentseed/changes/archive/`.

## Behaviour

1. Apply the delta content into the corresponding living `spec.md` files.
2. Move the change directory to archive.
3. Confirm the living specs are now the new source of truth.
