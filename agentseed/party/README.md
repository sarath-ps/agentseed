# Party Mode Sessions

Optional location for:

- Session summaries
- Persistent party memory (dynamics + open threads)
- Saved party configurations / casts

Keep these lightweight. Prefer linking to decisions and new changes under `agentseed/changes/` rather than storing full transcripts.
