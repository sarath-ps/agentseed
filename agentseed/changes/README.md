# Changes

Active change proposals live here.

Each change has an ID and contains:

- `proposal.md` – what and why
- `design.md` – technical decisions
- `tasks.md` – implementation breakdown
- `specs/` – deltas against living specs

Workflow: explore → propose → apply → verify → archive

Archived changes move to `changes/archive/`.
